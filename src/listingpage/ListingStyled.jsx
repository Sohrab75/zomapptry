import { Container } from "react-bootstrap";
import { styled } from "styled-components";

const ListingStyled = styled(Container)`
    div.card{
        width:300px;
    }
`;

export default ListingStyled